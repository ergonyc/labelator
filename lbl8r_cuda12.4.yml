#mamba env create -f lbl8r_py12.yml 

name: lbl8r-cuda12.4
channels:
- conda-forge
- pytorch
- nvidia
dependencies:
- python=3.12
- pytorch
- torchvision
- pytorch-cuda=12.4
- jax 
- jaxlib #=*=*cuda*
- scanpy
- scvi-tools
- python-igraph
- leidenalg
- pandas
- pyarrow
- tables
- scikit-misc
- ipykernel
- ipywidgets
- notebook
# - xgboost
- click
- pip
- pip:
  - scib-metrics
  - pymde
  - harmonypy
  - faiss-gpu-cu12=1.9.0
  - muon 
# post:
#   - pip install --upgrade "jax[cuda12]"  # This uses the upgrade flag


# mamba create -n scvi0 python=3.12 pip

# mamba activate scvi0
# mamba install pytorch torchvision torchaudio pytorch-cuda=12.4 -c pytorch -c nvidia
# mamba install -c conda-forge scvi-tools 

# mamba install -c conda-forge jax jaxlib scanpy scikit-learn pandas anndata pyarrow

# mamba install -c conda-forge click xgboost

# pip install pymde

# pip install scikit-misc 
# pip install tables
# pip install scib-metrics
# pip install harmonypy
# pip install muon
# pip install faiss-gpu-cu12 
